import Layout from '../../components/Layout';
import fetch from 'isomorphic-unfetch';

const Post = ({ show }) => {

    return (
        <Layout>
            <p>Blawg post content.</p>
        </Layout>
    );
};

export default Post;
